const { ServiceConfiguration } = require('../../types/config');

const config = {
  domainName: 'ranchosanpancho.com',
  port: 4000,
  database: {
    database: 'crm',
  },
};

module.exports = { config };
