const Service = {
  CRM: 'crm',
};

const ServiceType = {
  PRODUCTION: 'production',
  STAGING: 'staging',
  DEVELOPMENT: 'development',
};

const DeploymentType = {
  PRODUCTION: 'production',
  STAGING: 'staging',
  DEVELOPMENT: 'development',
};

module.exports = {
  Service,
  ServiceType,
  DeploymentType,
};
