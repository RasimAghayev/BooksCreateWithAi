var x, y T5
t5 = Add5(x, y)
}
}
The benchmark results:
Benchmark_Add4-4
2.649 ns/op
Benchmark_Add5-4
19.15 ns/op
The //go:noinline compiler directives used here are to prevent the calls to the two function from
being inlined. If the directives are removed, the Add4 function will become even more performant.
2.7
Value copy scenarios
In Go programming, besides value assignments, there are also several other operations involving
value copying:
• value boxing (converting non-interface values into interfaces).
• parameter passing and result returning in function calls.
• receiving values from and sending values to channels.
• put entries into maps.
• append elements to slices.
• iterate container elements/entries with for-range loop code blocks.
The following are several examples which show the costs of copying some large values.
Example 1:
package copycost
import "testing"
const N = 1024
//go:noinline
func Sum_RangeArray(a [N]int) (r int) {
for _, v := range a {
r += v
}
return
}
//go:noinline
func Sum_RangeArrayPtr1(a *[N]int) (r int) {
for _, v := range *a {
r += v
}
return
}
//go:noinline
func Sum_RangeArrayPtr2(a *[N]int) (r int) {
for _, v := range a {
r += v
16
