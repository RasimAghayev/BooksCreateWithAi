Hash and Maps
44
1
package main
2
3
import (
4
"fmt"
5
)
6
7
func main() {
8
var basket map[string]int
9
10
basket = make(map[string]int)
11
12
basket["apple"] = 1
13
basket["kale"] = 2
14
15
produce, exists := basket["cabbage"]
16
17
if !exists {
18
fmt.Println("Item does not exists")
19
} else {
20
fmt.Printf("%+v exists", produce)
21
}
22
}
1
$ go run map_read_non_present_key_2.go
2
3
Item does not exists
with zero return values, you can do this operation safely without type checking
1
basket["cabbage"]++
If we need to create a more complex map that uses a struct, then a pointer is required to initialize the
map, and the default return value will be nil. nil will be the default values for functions, interfaces,
slices, pointers, channels, and maps.
For example,
