Implementing Dependency Injection
Utilizing DI in Go doesn't necessarily require complex frameworks. Go's
interfaces and the language's composability make DI simpler and
straightforward.
Consider a simple example relating to our bookstore:
type BookRepository interface {
   GetByID(id int) (*Book, error)
}
type SQLBookRepository struct {
   db *sql.DB
}
func (s *SQLBookRepository) GetByID(id int) (*Book, error) {
   // Implementation using SQL database
}
type BookService struct {
   repo BookRepository
}
func NewBookService(r BookRepository) *BookService {
   return &BookService{repo: r}
}
func main() {
    db := //... initialization of the database
   sqlRepo := &SQLBookRepository{db: db}
