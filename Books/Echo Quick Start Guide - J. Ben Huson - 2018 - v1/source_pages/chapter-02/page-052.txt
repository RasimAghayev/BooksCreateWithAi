A brief commentary showing the reasoning for the structure is as follows:
bindings
The bindings directory will hold all of the protocol-specific data
bindings for the application, such as form submission, query string,
and JSON representations of application input
It is fairly common to see generic application data models used for
request and response types, but it is the opinion of the author that this
causes issues when a developer wishes to version API definitions
cmd
